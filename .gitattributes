# Legacy Python app (superseded by native/, the shipping Swift/SwiftUI app;
# kept only for reference and the macOS 12-13 fallback build). Excluding it
# from GitHub's language stats keeps the repo correctly classified as a
# Swift project instead of Python. `*` alone only matches immediate
# children, so these must be `**` to actually cover the nested files
# (e.g. voiceflow/llm/openai_backend.py).
voiceflow/** linguist-vendored

# Marketing website — not representative of the app's implementation
# language either.
docs/** linguist-documentation

# Test suite and build tooling are almost entirely pytest coverage for the
# legacy voiceflow/ app and docs/ build scripts (see AGENTS.md), not the
# shipping Swift app — exclude for the same reason.
tests/** linguist-vendored
scripts/** linguist-vendored
